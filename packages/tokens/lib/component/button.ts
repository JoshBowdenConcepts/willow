import { palette } from '../palette'

export const button = {
	primary: {
		bgDisabled: palette.teal[400],
		bgDefault: palette.teal[600],
		bgHover: palette.teal[700],
		bgFocus: palette.teal[800],
	},
}
